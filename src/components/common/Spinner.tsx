import { JSX } from "react";

export default function Spinner(): JSX.Element {
    return (
        <div className="aaravpos-spinner-wrapper">
            <div className="aaravpos-spinner" />
        </div>
    );
}